# 数据一致性验证工具使用说明

## 简介

`validate_data_consistency.py` 是一个数据一致性验证工具，用于检查K线数据和分析结果之间的时间一致性，确保分析使用的是最新的K线数据。

## 功能特性

1. **多周期K线时间差验证**：分别检查 5m/1h/4h 三个周期的K线数据与分析时间的时间差
2. **数据缺失检测**：查找存在分析结果但缺少对应K线数据的情况
3. **时间延迟统计**：计算平均延迟、最大延迟、P95延迟、中位数延迟
4. **K线数据覆盖率检查**：验证每个币种在不同周期的数据完整性
5. **告警汇总**：自动检测超过阈值的延迟和数据缺失问题

## 安装依赖

本项目使用 `uv` 作为包管理器。确保已安装所有依赖：

```bash
cd /Users/test/Downloads/hyperliquid-pair-hype-purr-analyze
uv sync
```

## 使用方法

### 基本用法

验证最近1小时的数据（默认）：

```bash
uv run python validate_data_consistency.py
```

### 指定时间窗口

验证最近24小时的数据：

```bash
uv run python validate_data_consistency.py --hours 24
```

验证最近7天的覆盖率：

```bash
uv run python validate_data_consistency.py --days 7
```

### 指定币种

验证特定币种：

```bash
uv run python validate_data_consistency.py --symbol "HYPE/USDC:USDC"
```

验证特定币种最近24小时的数据：

```bash
uv run python validate_data_consistency.py --hours 24 --symbol "PURR/USDC:USDC"
```

### 保存报告

将报告保存到文件：

```bash
uv run python validate_data_consistency.py --output report.txt
```

完整示例（指定币种、时间窗口并保存报告）：

```bash
uv run python validate_data_consistency.py \
  --hours 24 \
  --days 30 \
  --symbol "HYPE/USDC:USDC" \
  --output validation_report_$(date +%Y%m%d).txt
```

## 命令行参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--hours` | int | 1 | 延迟统计和数据缺失检测的时间窗口（小时） |
| `--days` | int | 7 | 覆盖率检查的时间窗口（天） |
| `--symbol` | str | None | 指定要验证的币种（可选） |
| `--output` | str | None | 输出报告到文件（可选） |

## 报告示例

```
============================================================
数据一致性验证报告
============================================================
生成时间: 2026-01-29 12:48:20

1. 多周期时间延迟统计（最近1小时）
------------------------------------------------------------
   5m : 平均 11348.73秒, 中位数 14905.50秒, P95 22024.31秒, 最大 25308.30秒 (543 条记录)
   1h : 平均 13836.02秒, 中位数 17525.95秒, P95 24724.31秒, 最大 28008.30秒 (543 条记录)
   4h : 平均 17303.43秒, 中位数 21125.95秒, P95 28324.31秒, 最大 31608.30秒 (543 条记录)

2. 数据缺失检测（最近1小时）
------------------------------------------------------------
   ⚠️  发现 496 条数据缺失记录:
      - XAI/USDC:USDC @ 2026-01-29 12:46:48.301241+00:00: 只有 0 个周期
      - NEO/USDC:USDC @ 2026-01-29 12:44:37.753810+00:00: 只有 0 个周期
      ... 还有 494 条记录

3. K线数据覆盖率（最近7天）
------------------------------------------------------------
   HYPE/USDC:USDC:
      5m : ✅ 100.35% (288 / 287)
      1h : ✅ 104.35% (24 / 23)
      4h : ✅ 120.00% (6 / 5)
   PURR/USDC:USDC:
      5m : ✅ 99.82% (286 / 287)
      1h : ✅ 104.35% (24 / 23)
      4h : ✅ 120.00% (6 / 5)

⚠️ 告警汇总
------------------------------------------------------------
   - 延迟过大: 5m 周期最大延迟达到 25308.3 秒
   - 延迟过大: 1h 周期最大延迟达到 28008.3 秒
   - 延迟过大: 4h 周期最大延迟达到 31608.3 秒
   - 数据缺失: XAI/USDC:USDC 在 2026-01-29 12:46:48 只有 0 个周期的数据

============================================================
```

## 告警阈值

- **延迟告警**：当最大延迟超过 60 秒时触发
- **覆盖率告警**：当数据覆盖率低于 95% 时触发
- **数据缺失告警**：当某个分析结果缺少对应的K线数据时触发

## 注意事项

1. **数据库连接**：确保 TimescaleDB 正在运行且配置正确（参见 `utils/config.py`）
2. **执行时间**：查询大量数据时可能需要较长时间，建议先使用较短的时间窗口测试
3. **覆盖率 >100%**：这是正常现象，可能是由于时区差异或数据重叠导致
4. **线程清理警告**：脚本结束时可能出现 `PythonFinalizationError`，这不影响功能，是连接池清理时的正常现象

## 故障排查

### 数据库连接失败

```bash
# 检查 TimescaleDB 是否运行
docker ps | grep timescaledb

# 检查数据库配置
cat utils/config.py | grep TIMESCALEDB
```

### 无数据返回

- 检查时间窗口是否过小
- 检查指定的币种是否存在于数据库中
- 验证是否有分析结果记录

### 查询超时

- 减少时间窗口（如从 24 小时改为 1 小时）
- 指定特定币种进行验证
- 检查数据库索引是否正常

## 相关文档

- [数据库设计文档](docs/MODULE1_DATABASE_INFRASTRUCTURE.md)
- [数据访问层文档](docs/MODULE2_DATABASE_ACCESS_LAYER.md)
- [实时数据流文档](docs/MODULE3_REALTIME_DATAFLOW.md)

## 技术实现

- **数据库**: TimescaleDB (PostgreSQL 扩展)
- **ORM**: psycopg 3.x
- **查询优化**: 使用 LATERAL JOIN 和 PERCENTILE_CONT 实现高效统计
- **连接池**: 基于 psycopg_pool 的连接池管理

## 维护者

Claude Code (2026-01-29)
