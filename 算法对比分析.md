# NOT/USDC:USDC 三种方法的算法对比分析

## 📊 从代码层面分析差异

根据 `multi_coins4.py` 第629-638行的代码：

```python
# 协整检验（基于配置周期数据）(老方案)，全量数据
ols_params = DelayCorrelationAnalyzer._calculate_cointegration_params(
    base_prices, alt_prices, coin=coin, base_symbol=self.base_symbol
)
cointegration_status_total_period = self.cointegration_analysis(ols_params, 'old', coin, stats_period_key)

# 双窗口策略：OLS回归使用长窗口（稳定），统计量使用短窗口（敏感）。
cointegration_result = DelayCorrelationAnalyzer.price_diff_spread_ols_window(
    base_prices, alt_prices, beta_window, zscore_window
)
cointegration_status_short_period = self.cointegration_analysis(cointegration_result, 'new', coin, stats_period_key)
```

---

## 🔍 三种方法详细对比

### 1️⃣ Old方法

#### 调用函数
```python
_calculate_cointegration_params(base_prices, alt_prices, ...)
```

#### 算法（第366-407行）
```python
def _calculate_cointegration_params(base_prices, alt_prices, ...):
    """
    使用OLS回归计算协整参数（验证性函数）
    注意：此函数存在 look-ahead bias（使用全样本），仅用于事后验证
    """
    # 1. 使用 **全量数据** 进行OLS回归
    log_base = np.log(base_prices).values.reshape(-1, 1)
    log_alt = np.log(alt_prices).values
    
    # 2. OLS回归
    model = LinearRegression()
    model.fit(log_base, log_alt)
    alpha = model.intercept_
    beta = model.coef_[0]
    
    # 3. 构建价差
    log_base_series = np.log(base_prices)
    log_alt_series = np.log(alt_prices)
    spread = log_alt_series - (alpha + beta * log_base_series)
    
    # 4. ADF检验（全量数据）
    adf_result = adfuller(spread.values, autolag='AIC')
    adf_pvalue = adf_result[1]
    
    return {'alpha': alpha, 'beta': beta, 'spread': spread, 'adf_pvalue': adf_pvalue}
```

#### 特点
- ✅ **数据范围**: 使用**完整60天的全部数据**（约360期）
- ✅ **方法**: 标准的Engle-Granger两步法
- ✅ **目的**: 事后验证，判断历史上是否存在协整关系
- ⚠️ **缺点**: 存在look-ahead bias（向前偏差），不适合实时交易

#### NOT的结果
- **ADF p值**: 0.0341 ✅（刚好通过）
- **β系数**: 2.1647
- **数据**: 完整360期

---

### 2️⃣ New方法

#### 调用函数
```python
price_diff_spread_ols_window(base_prices, alt_prices, beta_window, zscore_window)
```

#### 算法（第449-489行）
```python
def price_diff_spread_ols_window(base_prices, alt_prices, beta_window=100, zscore_window=30):
    """
    双窗口策略：OLS回归使用长窗口（稳定），统计量使用短窗口（敏感）
    避免look-ahead bias
    """
    # 1. 数据切片：取足够计算OLS回归和统计量的数据
    data_window = max(beta_window, zscore_window)  # 通常是100
    recent_base_full = base_prices.iloc[-data_window:]  # 最近100期
    recent_alt_full = alt_prices.iloc[-data_window:]
    
    # 2. OLS回归计算协整参数
    # 使用前 beta_window-1 个点计算OLS参数（避免 look-ahead bias）
    ols_base = recent_base_full.iloc[:-1]  # 前99期
    ols_alt = recent_alt_full.iloc[:-1]
    
    log_base_ols = np.log(ols_base).values.reshape(-1, 1)
    log_alt_ols = np.log(ols_alt).values
    
    # OLS回归
    model = LinearRegression()
    model.fit(log_base_ols, log_alt_ols)
    alpha = model.intercept_
    beta_ols = model.coef_[0]
    
    # 3. 价差构建（用于ADF检验：使用100期）
    log_base_full = np.log(recent_base_full)  # 全部100期
    log_alt_full = np.log(recent_alt_full)
    spread_full = log_alt_full - (alpha + beta_ols * log_base_full)
    
    # 4. ADF检验价差平稳性（100期数据）
    adf_result = adfuller(spread_full.values, autolag='AIC')
    adf_pvalue = adf_result[1]
    
    # 5. 价差构建（用于Z-score计算：使用短窗口30期）
    recent_base = recent_base_full.iloc[-zscore_window:]
    recent_alt = recent_alt_full.iloc[-zscore_window:]
    log_base = np.log(recent_base)
    log_alt = np.log(recent_alt)
    spread = log_alt - (alpha + beta_ols * log_base)
    
    return {'alpha': alpha, 'beta': beta_ols, 'spread': spread, 'adf_pvalue': adf_pvalue}
```

#### 特点
- ✅ **数据范围**: 使用**最近100期数据**
  - OLS回归用前99期
  - ADF检验用100期
  - Z-score计算用最近30期
- ✅ **方法**: 双窗口策略，避免look-ahead bias
- ✅ **目的**: 实时交易，评估当前可交易性
- ⚠️ **更严格**: 因为数据量少且避免了前向偏差

#### NOT的结果
- **ADF p值**: 0.0651 ❌（未通过）
- **β系数**: 3.0519
- **数据**: 最近100期

---

### 3️⃣ 协整健康监控（100期窗口）

#### 调用函数
```python
monitor_short = CointegrationHealthMonitor(window=100, ...)
result_short = monitor_short.update(log_base_series, log_alt_series)
```

#### 算法（utils/coingetation_more_check.py 第49-120行）
```python
class CointegrationHealthMonitor:
    def update(self, logA: pd.Series, logB: pd.Series):
        """更新健康监控数据"""
        # 1. 取最近window期数据
        logA = logA[-self.window:]  # 最近100期
        logB = logB[-self.window:]
        
        # 2. 估算β和价差
        beta, spread, beta_model = self._estimate_beta_and_spread(logA, logB)
        
        # 3. 计算ADF得分
        adf_score, adf_pvalue = self._adf_strength_score(spread)
        
        # 4. 计算半衰期得分
        halflife_score, halflife, halflife_reason, ar1_model = self._halflife_score(spread)
        
        # 5. 计算稳定性得分
        stability_score, stability_details = self._stability_score(spread, logA, logB)
        
        # 6. 综合得分（权重：ADF 40%, 半衰期 30%, 稳定性 30%）
        health_score = 0.4 * adf_score + 0.3 * halflife_score + 0.3 * stability_score
        
        return result
    
    def _estimate_beta_and_spread(self, logA, logB):
        """估算协整系数和价差"""
        # 使用statsmodels的OLS（带常数项）
        X = sm.add_constant(logB)
        model = sm.OLS(logA, X).fit()
        beta = model.params.iloc[1]
        spread = logA - beta * logB  # 注意：这里没有减去alpha（常数项）
        return beta, spread, model
    
    def _adf_strength_score(self, spread):
        """ADF平稳性检验评分 (0-40分)"""
        adf_stat, pvalue, *_ = adfuller(spread, autolag="AIC")
        
        if pvalue > 0.1:
            score = 0
        elif pvalue < 0.01:
            score = 40
        else:
            # 线性插值
            score = 40 * (0.1 - pvalue) / (0.1 - 0.01)
        
        return score, pvalue
```

#### 特点
- ✅ **数据范围**: 使用**最近100期数据**
- ✅ **方法**: 综合评分系统（ADF + 半衰期 + 稳定性）
- ✅ **价差计算**: `spread = logA - beta * logB`（不减alpha）
- ✅ **目的**: 健康度监控，提供综合评估
- ⚠️ **关键差异**: 使用statsmodels的OLS（更严格）

#### NOT的结果
- **ADF p值**: 0.0215 ✅（通过）
- **综合得分**: 25.96（HEALTHY）
- **数据**: 最近100期

---

## 🎯 核心差异总结

| 维度 | Old方法 | New方法 | 健康监控(100期) |
|------|---------|---------|-----------------|
| **数据量** | 完整360期 | 最近100期 | 最近100期 |
| **OLS回归** | 全量数据 | 前99期 | 全部100期 |
| **ADF检验数据** | 全量360期 | 100期 | 100期 |
| **β系数** | 2.1647 | 3.0519 | 不同算法 |
| **价差公式** | log(alt) - α - β×log(base) | log(alt) - α - β×log(base) | log(alt) - β×log(base) |
| **ADF p值** | 0.0341 | 0.0651 | 0.0215 |
| **是否通过** | ✅ | ❌ | ✅ |
| **Look-ahead bias** | 有 | 无 | 无 |
| **OLS库** | LinearRegression | LinearRegression | sm.OLS（更严格） |

---

## 💡 为什么同样100期数据，结果不同？

### New方法 vs 健康监控（都用100期）

虽然都使用100期数据，但有3个关键差异：

#### 1️⃣ OLS回归范围不同
```python
# New方法：使用前99期回归，然后用100期检验
ols_base = recent_base_full.iloc[:-1]  # 前99期
# ADF检验用100期

# 健康监控：使用全部100期回归和检验
logA = logA[-self.window:]  # 全部100期
```

#### 2️⃣ 价差计算公式不同
```python
# New方法：
spread = log_alt - (alpha + beta * log_base)

# 健康监控：
spread = logA - beta * logB  # 不减alpha（常数项）
```

#### 3️⃣ OLS实现库不同
```python
# New方法：使用sklearn的LinearRegression
from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(log_base_ols, log_alt_ols)

# 健康监控：使用statsmodels的OLS（更严格的统计方法）
import statsmodels.api as sm
X = sm.add_constant(logB)
model = sm.OLS(logA, X).fit()
```

**statsmodels vs sklearn的差异：**
- statsmodels：统计学严格，提供更多统计量（p值、R²等）
- sklearn：机器学习简化版，侧重预测

---

## 📊 数据范围的影响

```
NOT/USDC:USDC 60天协整强度变化（推测）：

强度
 ↑
 │                              ┌─────────────┐
 │                          ┌───┤  100期窗口  │ p=0.0215 ✅
 │                      ┌───┤   └─────────────┘
 │                  ┌───┤   │
 │              ┌───┤   │   │
 │          ┌───┤不 │一 │良 │优
 │      ┌───┤稳 │稳 │般 │好 │秀
 │  ┌───┤定 │定 │   │   │   
 └──┴───┴───┴───┴───┴───┴─────────────────────→ 时间
 0     60   120  180  240  300  360期
 
 └──────────── Old: 360期 ────────┘ p=0.0341 ✅
              └── New: 100期 ──┘   p=0.0651 ❌
              └── 监控: 100期 ──┘  p=0.0215 ✅
```

**为什么同样100期，New方法失败？**

1. **回归数据**: New方法用前99期回归，可能没有完全捕捉到稳定期
2. **价差公式**: 减了alpha可能导致价差序列不同
3. **实现差异**: sklearn vs statsmodels计算出的β系数略有差异

---

## 🎓 最终答案

### 问：为什么New方法(p=0.0651)未通过，但健康监控(p=0.0215)通过了？

**答：虽然都用100期数据，但算法细节不同！**

1. **数据使用方式**
   - New: 用前99期回归 → β=3.0519
   - 监控: 用全100期回归 → β不同

2. **价差计算**
   - New: `spread = log_alt - (α + β × log_base)`
   - 监控: `spread = log_alt - β × log_base`（无α项）

3. **OLS库差异**
   - New: sklearn.LinearRegression（简化版）
   - 监控: statsmodels.OLS（统计严格版）

4. **结果**
   - New方法算出的价差序列ADF p值=0.0651（刚好不及格）
   - 健康监控算出的价差序列ADF p值=0.0215（通过）

**本质原因**: 
- 细节决定成败！
- 看似都用100期数据，但具体怎么用（回归范围、公式、库）都不同
- 导致最终计算的价差序列不同，ADF检验结果自然不同

---

## 📋 给交易者的建议

1. **以健康监控为准**: 
   - 健康监控使用综合评分（ADF + 半衰期 + 稳定性）
   - 更全面地评估协整关系质量

2. **参考Old方法**: 
   - 了解历史整体情况
   - Old通过说明历史上存在协整

3. **注意New方法警告**:
   - New方法p=0.0651接近临界值
   - 说明当前100期处于"临界状态"
   - 需要密切监控

4. **综合判断**:
   - Old(0.0341)✅ + New(0.0651)❌ + 监控(0.0215)✅
   - 结论：可以交易，但要小心（属于"及格边缘"）

