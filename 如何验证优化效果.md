# 如何验证自适应协整检验优化效果

## 🎯 一键验证

### 方法1: 运行现有程序，查看日志

```bash
# 运行multi_coins4.py
python multi_coins4.py

# 或使用uv（如果配置了）
uv run python multi_coins4.py
```

**查看要点**:
1. 查找日志中的 `Old方法` 和 `New方法` 行
2. 注意 `模型类型` 字段（standard_EG 或 no_intercept）
3. 注意 `α显著性: p=` 值
4. 对比 ADF p值

### 方法2: 统计模型使用情况

```bash
# 在日志文件中统计
grep "模型类型: standard_EG" multi_coins.log | wc -l
grep "模型类型: no_intercept" multi_coins.log | wc -l
```

## 📊 预期日志输出

### 典型输出示例

```
协整参数 | 币种: NOT/USDC:USDC | α=-5.2134 (p=0.0823) | β=3.0519 (p=0.0000) | R²=0.8234 | 模型: no_intercept | ADF p=0.0341

Old方法 | 币种: NOT/USDC:USDC | 模型类型: no_intercept | α显著性: p=0.0823 | β显著性: p=0.0000 | R²=0.8234

New方法 | 币种: NOT/USDC:USDC | 模型类型: no_intercept | α显著性: p=0.1534 | β显著性: p=0.0000 | R²=0.7891
```

### 解读

1. **α显著性 p=0.0823**
   - p > 0.05，α不显著
   - ✅ 自动选择 `no_intercept` 模型
   - ✅ 价差计算不减α，避免引入噪音

2. **模型类型: no_intercept**
   - 使用无常数项模型
   - 价差 = log(ALT) - β×log(BASE)

3. **ADF p值**
   - 对比优化前后的变化
   - 预期: α不显著时，优化后p值更小

## 🔍 详细验证步骤

### 步骤1: 找一个α不显著的币种对

运行程序，找到 `α显著性: p=0.XX` 且 p>0.05 的币种对。

**预期**:
- ✅ 模型类型应该是 `no_intercept`
- ✅ ADF p值应该比之前更小（如果有历史数据对比）

### 步骤2: 找一个α显著的币种对

找到 `α显著性: p=0.XX` 且 p<0.05 的币种对。

**预期**:
- ✅ 模型类型应该是 `standard_EG`
- ✅ ADF p值应该与传统方法相近（因为都减α）

### 步骤3: 统计整体分布

```bash
# 假设日志文件是 multi_coins.log

# 统计标准EG模型
echo "标准EG模型 (α显著):"
grep "Old方法.*standard_EG" multi_coins.log | wc -l

# 统计无常数项模型
echo "无常数项模型 (α不显著):"
grep "Old方法.*no_intercept" multi_coins.log | wc -l
```

**预期**:
- 大部分币种对应该是 `no_intercept`（α不显著更常见）
- 少部分币种对是 `standard_EG`（α显著的情况）

### 步骤4: 对比NOT/USDC的结果

如果有历史日志，对比NOT/USDC的ADF p值变化。

**优化前**（假设）:
```
Old: ADF p=0.0341 (强制减α)
New: ADF p=0.0651 (强制减α，未通过)
```

**优化后**（预期）:
```
Old: ADF p≈0.025 (α不显著，不减α，改善)
New: ADF p≈0.045 (α不显著，不减α，改善并通过)
```

## 📈 成功指标

### ✅ 优化成功的标志

1. **日志包含模型类型**
   ```
   Old方法 | ... | 模型类型: no_intercept | ...
   ```

2. **α显著性正确判断**
   - p<0.05 → standard_EG
   - p≥0.05 → no_intercept

3. **统计信息完整**
   ```
   α显著性: p=0.XXXX | β显著性: p=0.XXXX | R²=0.XXXX
   ```

4. **ADF p值改善**
   - α不显著的币种对，ADF p值应该更小

### ❌ 可能的问题

1. **没有模型类型输出**
   - 检查代码是否正确实现
   - 检查日志级别是否正确

2. **所有币种对都是同一种模型**
   - 如果全是 standard_EG → 检查阈值是否太宽松
   - 如果全是 no_intercept → 正常（大部分α不显著）

3. **ADF p值没有改善**
   - 可能是α本来就显著，优化对其无影响
   - 或者样本数据特殊

## 🎨 可视化分析（可选）

### Python脚本分析日志

```python
import re
import pandas as pd

# 读取日志文件
with open('multi_coins.log', 'r') as f:
    lines = f.readlines()

# 提取Old方法的数据
pattern = r'Old方法 \| 币种: (.*?) \| 模型类型: (.*?) \| α显著性: p=(.*?) \|'
data = []
for line in lines:
    match = re.search(pattern, line)
    if match:
        data.append({
            'coin': match.group(1),
            'model': match.group(2),
            'alpha_p': float(match.group(3))
        })

df = pd.DataFrame(data)

# 统计
print(f"总币种对数: {len(df)}")
print(f"标准EG模型: {(df['model'] == 'standard_EG').sum()}")
print(f"无常数项模型: {(df['model'] == 'no_intercept').sum()}")
print(f"\nα显著 (p<0.05): {(df['alpha_p'] < 0.05).sum()}")
print(f"α不显著 (p≥0.05): {(df['alpha_p'] >= 0.05).sum()}")

# 验证一致性
assert (df['alpha_p'] < 0.05).sum() == (df['model'] == 'standard_EG').sum()
print("\n✅ 模型选择与α显著性完全一致！")
```

## 📋 检查清单

运行程序后，验证以下各项：

- [ ] 日志中有 `Old方法 | ... | 模型类型:` 输出
- [ ] 日志中有 `New方法 | ... | 模型类型:` 输出
- [ ] 日志中有 `α显著性: p=` 输出
- [ ] 日志中有 `β显著性: p=` 输出
- [ ] 日志中有 `R²=` 输出
- [ ] α显著时（p<0.05），模型类型是 `standard_EG`
- [ ] α不显著时（p≥0.05），模型类型是 `no_intercept`
- [ ] 大部分币种对是 `no_intercept` 模型
- [ ] ADF p值相比之前有改善（如果有历史数据）

**全部勾选 → 优化成功！** ✅

## 💡 快速诊断

### 问题1: 没有看到模型类型输出

**原因**: 日志级别太高  
**解决**: 检查日志级别是否设置为 INFO 或 DEBUG

### 问题2: 所有币种对都是 standard_EG

**原因**: 不太可能，除非数据特殊  
**检查**: 
- α显著性阈值是否正确（应该是0.05）
- 数据是否有问题

### 问题3: 程序报错

**原因**: 可能缺少依赖或环境问题  
**检查**:
- statsmodels 是否安装
- Python版本是否兼容

## 🚀 下一步

验证成功后：

1. ✅ 收集一周的数据，观察模型选择分布
2. ✅ 统计ADF p值改善的币种对数量
3. ✅ 根据实际效果调整显著性阈值（如果需要）
4. ✅ 将统计结果记录到文档

## 📞 需要帮助？

如果验证过程中遇到问题：

1. 查看 `ADAPTIVE_COINTEGRATION_OPTIMIZATION.md` 了解详细原理
2. 查看 `OPTIMIZATION_QUICK_GUIDE.md` 了解使用方法
3. 查看 `IMPLEMENTATION_SUMMARY.md` 了解实施细节

---

**祝验证顺利！** 🎉
